import array from './index';

test('数组包含Banana', () => {
  const result = array('Apple', 'Banana');
  const value = 'Banana';

  // <--start
  // TODO: 给出正确的assertion
  expect(result);
  // --end->
});
